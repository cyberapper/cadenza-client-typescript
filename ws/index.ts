/**
 * WebSocket client re-exports from centrifuge-js.
 *
 * Usage:
 *   import { Centrifuge } from '@cyberapper/cadenza-client/ws';
 *
 *   const client = new Centrifuge('ws://localhost:8000/connection/websocket');
 *   client.connect();
 */

export * from 'centrifuge';
